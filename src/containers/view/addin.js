import * as React from 'react';
import styles from './index.less';

export default class AddIn extends React.Component {
    render() {
        return (
            <div>
                <form>

                </form>
            </div>
        )
    }
}